image: node:6.6.0

generate_js:
  type: build
  script:
  - npm install -g angular-cli
  - npm install
  - ng build
  only:
  - master
  
move_files:
  type: deploy
  script:
  - cp dist/* /home/anthony/TEST_DEP
  only:
  - master
  